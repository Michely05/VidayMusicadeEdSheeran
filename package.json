{
  "name": "my-project",
  "source": "src/index.html",
  "scripts": {
    "start": "npm-run-all clean parcel:main",
    "build": "parcel build index.html",
    "parcel:dev": "parcel",
    "parcel:build": "parcel build",
    "clean": "rimraf dist .parcel-cache"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.12.0",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^5.0.7"
  }
}
